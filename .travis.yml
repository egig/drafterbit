language: node_js
node_js:
  - 12
before_script:
  - npm install -g codecov
  - npm install -g lerna
  - npm run bootstrap
script:
  - lerna run test
  - lerna run coverage:lcov
  - codecov
branches:
  only:
    - develop
